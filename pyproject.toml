[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sfplot"
version = "0.1.0"
description = "For plotting the Search and Find Plot (SFplot)."
readme = "README.rst"
authors = [
  {name = "Taobo Hu", email = "taobo.hu@scilifelab.se"}
]
maintainers = [
  {name = "Taobo Hu", email = "taobo.hu@scilifelab.se"}
]
classifiers = [

]
license = {text = "MIT license"}
dependencies = [
  "typer"
]

[project.optional-dependencies]
dev = [
    "coverage",  # testing
    "mypy",  # linting
    "pytest",  # testing
    "ruff"  # linting
]

[project.urls]

bugs = "https://github.com/hutaobo/sfplot/issues"
changelog = "https://github.com/hutaobo/sfplot/blob/master/changelog.md"
homepage = "https://github.com/hutaobo/sfplot"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
"*" = ["*.*"]




# Mypy
# ----

[tool.mypy]
files = "."

# Use strict defaults
strict = true
warn_unreachable = true
warn_no_return = true

[[tool.mypy.overrides]]
# Don't require test functions to include types
module = "tests.*"
allow_untyped_defs = true
disable_error_code = "attr-defined"

