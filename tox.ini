[tox]
envlist = py39, py310, py311, ruff

[testenv:ruff]
basepython = python
deps = ruff
commands = ruff check src/sfplot tests

[testenv]
setenv =
    PYTHONPATH = {toxinidir}/src

deps =
    pytest

commands = pytest tests
