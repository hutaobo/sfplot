## 功能

### 生成 Cluster Distance Heatmap

```python
from sfplot import generate_cluster_distance_heatmap

base_path = "/path/to/data"
sample = "sample_name"
output_dir = "/path/to/output"

generate_cluster_distance_heatmap(base_path, sample, output_dir)
